# Pinned to what was verified locally on Python 3.12.
#
# Deliberately CPU-only and small. mediapipe ships the 3.6MB landmark model
# separately (downloaded on first run, baked into the image below), and
# opencv-python-HEADLESS is the server build — the normal opencv-python pulls in
# GUI/X11 libraries that a container has no use for and that add ~200MB.
fastapi==0.117.1
uvicorn[standard]==0.32.1
pydantic==2.10.4
mediapipe==1.0.0
opencv-python-headless==5.0.0.88
numpy==2.4.6
python-multipart==0.0.20
