# Copy to .env on the VPS and fill in. Never commit the real file.
SITE_DOMAIN=skin.aestheticscentral.co.uk

POSTGRES_PASSWORD=
MINIO_ROOT_USER=aesthetics
MINIO_ROOT_PASSWORD=

ANTHROPIC_API_KEY=
NEXT_PUBLIC_CALENDAR_URL=
NEXT_PUBLIC_SITE_URL=https://skin.aestheticscentral.co.uk

# Optional, only if a diffusion worker is added later. The stack runs fully
# without it — the lift and the skin work are deterministic and need no GPU.
FAL_KEY=
